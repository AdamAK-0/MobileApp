<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:orientation="vertical"
        android:padding="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Image Preview -->
        <ImageView
            android:id="@+id/imgPreview"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_gravity="center"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_image"
            android:background="#E0E0E0"
            android:layout_marginBottom="10dp" />

        <!-- Select Image Button -->
        <Button
            android:id="@+id/btnSelectImage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/select_image"
            android:layout_marginBottom="20dp" />

        <!-- Internship Name -->
        <EditText
            android:id="@+id/internName"
            android:hint="@string/internship_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp" />

        <!-- Description -->
        <EditText
            android:id="@+id/internDesc"
            android:hint="@string/description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minLines="3"
            android:gravity="top"
            android:layout_marginTop="10dp" />

        <!-- Start Date -->
        <EditText
            android:id="@+id/internStart"
            android:hint="@string/start_date_yyyy_mm_dd"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:focusable="false"
            android:clickable="true"
            android:layout_marginTop="10dp" />

        <!-- End Date -->
        <EditText
            android:id="@+id/internEnd"
            android:hint="@string/end_date_yyyy_mm_dd"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:focusable="false"
            android:clickable="true"
            android:layout_marginTop="10dp" />

        <!-- Type Spinner -->
        <Spinner
            android:id="@+id/spinnerType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp" />

        <!-- Max Slots -->
        <EditText
            android:id="@+id/internSlots"
            android:hint="@string/max_slots"
            android:inputType="number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp" />

        <!-- Submit -->
        <Button
            android:id="@+id/btnSubmit"
            android:text="@string/add_internship"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="25dp" />

    </LinearLayout>
</ScrollView>
